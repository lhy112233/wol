#include "wol/config.hpp"

// The schema-specific TOML reader/writer lives in config.cpp. This translation
// unit is intentionally kept so the project layout can grow without changing
// build targets.
